# ===== Core =====
numpy>=1.24,<3.0
pandas>=2.1,<3.0
ccxt>=4.1.0,<5

# ===== Viz / App =====
streamlit>=1.33,<2
altair>=5.2,<6
watchdog>=4,<5          # auto-refresh CSV บน Streamlit (optional แต่แนะนำ)

# ===== Utils (optional) =====
python-dotenv>=1.0,<2   # ถ้าจะโหลด API key จาก .env
pyarrow>=14,<19         # ทำให้ pandas/st.dataframe ลื่นขึ้น (optional, ไฟล์ใหญ่)
